services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "8080:80" # NPM'e verdiğin portla aynı olmalı
    container_name: guidelonia_app
    deploy:
      resources:
        limits:
          memory: 512M # 21GB RAM'in küçük bir kısmını ayırıyoruz
